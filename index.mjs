/**
 * The umbrella Host plugin intentionally has no runtime side effects.
 * Its enabled Cordis loader row lets DSH discover this package's client bundle.
 */
export const name = 'dsh-plugins-4u'

export function apply() {}
